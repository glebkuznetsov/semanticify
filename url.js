/** A single URL object. */


/** Model of a url object. */
semanticify.Url = Backbone.Model.extend({

});


/** Model of a url object. */
semanticify.UrlCollection = Backbone.Collection.extend({
  model: semanticify.Url,
});


/** View for a single URL object. */
semanticify.UrlView = Backbone.View.extend({

});
